---
import { twMerge } from "tailwind-merge"

interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<section class="flex flex-col items-center">
  <div class={twMerge("w-full max-w-5xl px-16", className)}>
    <slot />
  </div>
</section>
